---
phase: 06-react-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - ui/src/components/StatsCards.tsx
  - ui/src/components/WorkerFleet.tsx
  - ui/src/components/QueueDepthChart.tsx
  - ui/src/components/WorkerSpeedChart.tsx
  - ui/src/components/SubmitJobForm.tsx
  - ui/src/components/LogFeed.tsx
  - ui/src/components/Header.tsx
  - ui/src/components/StatusPill.tsx
  - ui/src/lib/format.ts
autonomous: true
requirements:
  - REQ-018
  - REQ-019
  - REQ-021
  - REQ-022
  - REQ-024
user_setup: []

must_haves:
  truths:
    - "Stats cards show total / idle / busy workers, queue depth, jobs done, total jobs from stores"
    - "Worker fleet grid renders one chip per worker with status dot and short id"
    - "Queue depth chart renders a Recharts line chart over the last 2 minutes from queueDepthHistory"
    - "Worker speed chart renders a multi-line Recharts chart, one line per worker, showing busy-ratio over the last 2 minutes"
    - "Submit job form accepts F (2..100000) and C (1..10000), shows file-size preview, POSTs to /jobs and logs success/error"
    - "Log feed renders up to 200 lines with level filter buttons (all/info/warn/error) and clear button"
    - "Header shows connection status pill bound to useSystemStore.connectionStatus"
  artifacts:
    - path: "ui/src/components/StatsCards.tsx"
      provides: "Six stat cards driven by selectors"
      contains: "useSystemStore"
    - path: "ui/src/components/WorkerFleet.tsx"
      provides: "Worker chip grid"
      contains: "WorkerInfo"
    - path: "ui/src/components/QueueDepthChart.tsx"
      provides: "Recharts LineChart of queue depth over 2 min"
      contains: "LineChart"
    - path: "ui/src/components/WorkerSpeedChart.tsx"
      provides: "Multi-line Recharts chart of worker busy-ratio over 2 min"
      contains: "LineChart"
    - path: "ui/src/components/SubmitJobForm.tsx"
      provides: "F/C form with size preview that calls api.createJob"
      contains: "createJob"
    - path: "ui/src/components/LogFeed.tsx"
      provides: "Filterable log list bound to useLogStore"
      contains: "useLogStore"
    - path: "ui/src/components/Header.tsx"
      provides: "Title + connection status"
      contains: "StatusPill"
    - path: "ui/src/lib/format.ts"
      provides: "formatBytes, formatElapsed, shortId helpers"
      contains: "export function formatBytes"
  key_links:
    - from: "ui/src/components/SubmitJobForm.tsx"
      to: "ui/src/lib/api.ts createJob"
      via: "import + await"
      pattern: "createJob"
    - from: "ui/src/components/QueueDepthChart.tsx"
      to: "ui/src/store/useJobsStore.ts queueDepthHistory"
      via: "useJobsStore selector"
      pattern: "queueDepthHistory"
    - from: "ui/src/components/WorkerSpeedChart.tsx"
      to: "ui/src/store/useSystemStore.ts workerSpeedHistory"
      via: "useSystemStore selector"
      pattern: "workerSpeedHistory"
---

<objective>
Build the visual dashboard components (stats cards, worker fleet, charts, submit form, log feed, header) on top of the Zustand stores and typed API client created in Plan 01.

Purpose: Each component is independently testable and reads only from stores. App composition is deferred to Plan 04. JobsTable is owned by Plan 03.

Output: A set of presentational/container components that, when imported, render the parts of the dashboard described in the ROADMAP deliverables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/DMEAN-06-react-dashboard/06-01-SUMMARY.md
@ui/legacy.index.html
@ui/src/types/api.ts
@ui/src/types/sse.ts
@ui/src/store/useSystemStore.ts
@ui/src/store/useJobsStore.ts
@ui/src/store/useLogStore.ts
@ui/src/lib/api.ts

<interfaces>
<!-- All components MUST read state from stores via useSystemStore / useJobsStore / useLogStore hooks defined in Plan 01. -->
<!-- Charts use the queueDepthHistory and workerSpeedHistory time-series buffers maintained by those stores. -->
<!-- API mutations go through ui/src/lib/api.ts (createJob, patchWorkerCount). -->

Tailwind palette already configured in Plan 01: slate-900 (bg), slate-800 (cards), slate-700 (borders), slate-200 (text), accents indigo-500, emerald-500, amber-500, red-500. Match these to ui/legacy.index.html visual style.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Format helpers and Header/StatusPill</name>
  <files>ui/src/lib/format.ts, ui/src/components/StatusPill.tsx, ui/src/components/Header.tsx</files>
  <read_first>
    - ui/legacy.index.html (lines 468–475 formatElapsed; lines 548–552 formatBytes; lines 100–109 header markup; lines 13–17 status-dot styles)
    - ui/src/store/useSystemStore.ts (connectionStatus shape)
  </read_first>
  <action>
    Create `ui/src/lib/format.ts` exporting: `formatBytes(b: number): string` (B / KB / MB cutoffs at 1024 — match legacy `formatBytes`); `formatElapsed(start: string | null, end?: string | null): string` (returns `—` if no start; ms < 1000 → `${ms}ms`; ms < 60000 → `${(ms/1000).toFixed(1)}s`; otherwise `${Math.floor(ms/60000)}m ${Math.floor((ms%60000)/1000)}s`); `shortId(id: string, len = 8): string` returns first `len` chars; `formatTime(iso: string | number): string` returns localized HH:MM:SS. All accept `string | null` defensively. Create `ui/src/components/StatusPill.tsx` as a function component taking no props, reading `connectionStatus` via `useSystemStore`. Render a span with a colored dot (emerald-500 when 'connected', slate-500 when 'connecting', amber-500 when 'reconnecting') and label text. Use Tailwind classes. Create `ui/src/components/Header.tsx` rendering a fixed-height header: bg-slate-800, border-b border-slate-700, with title "⚡ Distributed Mean" on the left and `<StatusPill />` on the right. No state of its own.
  </action>
  <verify>
    <automated>cd ui &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File ui/src/lib/format.ts contains "export function formatBytes" and "export function formatElapsed" and "export function shortId"
    - File ui/src/components/StatusPill.tsx contains "useSystemStore" and "connectionStatus"
    - File ui/src/components/Header.tsx contains "StatusPill" and "Distributed Mean"
    - Command `cd ui &amp;&amp; npm run build` exits 0
    - Command `grep -n ": any\\|<any>\\| as any" ui/src/lib/format.ts ui/src/components/StatusPill.tsx ui/src/components/Header.tsx | wc -l` returns 0
  </acceptance_criteria>
  <done>format helpers and header components exist, typed, building cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: StatsCards, WorkerFleet, SubmitJobForm, LogFeed</name>
  <files>ui/src/components/StatsCards.tsx, ui/src/components/WorkerFleet.tsx, ui/src/components/SubmitJobForm.tsx, ui/src/components/LogFeed.tsx</files>
  <read_first>
    - ui/legacy.index.html (lines 112–138 stats markup; lines 140–144 worker fleet; lines 146–165 submit form; lines 190–202 log feed; lines 356–374 renderWorkers; lines 454–466 updateStats; lines 480–519 addLog/setFilter/clearLog; lines 530–588 submitJob)
    - ui/src/store/useSystemStore.ts and useJobsStore.ts and useLogStore.ts
    - ui/src/lib/api.ts (createJob, ApiError)
    - ui/src/lib/format.ts (formatBytes, shortId)
  </read_first>
  <action>
    Create `ui/src/components/StatsCards.tsx` rendering six cards in a responsive grid (grid-cols-2 sm:grid-cols-3 lg:grid-cols-6, gap-3): Workers (totals), Idle, Busy, Queue Depth, Jobs Done, Total Jobs. Read from `useSystemStore` (workers, queueDepth) and `useJobsStore` (jobs). Compute idle/busy as workers.filter(w=>w.status==='idle').length and 'busy'. Compute jobs done as `Object.values(jobs).filter(j=>j.status==='done').length` and total as `Object.values(jobs).length`. Color the value text per legacy: Idle emerald-400, Busy amber-400, Queue indigo-400, Done emerald-400. Each card is `<div class="rounded-lg bg-slate-800 border border-slate-700 p-4 text-center">`. Create `ui/src/components/WorkerFleet.tsx` reading `workers` from `useSystemStore`. Render a flex-wrap row of "chips" — one per worker. Each chip shows a colored dot (emerald for idle, amber for busy), `shortId(w.id)`, the status label, and if `w.currentTaskId` is set show `→ task:${shortId(currentTaskId,6)}`. If no workers, render placeholder text "No workers connected yet". Wrap the card with title h2 "Worker Fleet". Create `ui/src/components/SubmitJobForm.tsx` with local state `f` (default 20), `c` (default 100), `submitting` (boolean), `result` ({type:'success'|'error',message:string}|null). Render two number inputs (min/max per F 2..100000, C 1..10000), a primary submit button, a size preview line under the form using `formatBytes(f * c * 9)` plus `${Math.ceil(f/5)} batch(es)`. On submit: clamp/validate (f>=2 && c>=1), then call `createJob({F:f,C:c})`. On success: useJobsStore.getState().upsertJob({ id: data.jobId, f: data.f, c: data.c, status: data.status, batchCount: data.batchCount, completedBatches: 0, createdAt: data.createdAt, resultPath: null, error: null, updatedAt: data.createdAt, completedAt: null }); useLogStore.getState().addLine('info', `Submitted job ${shortId(data.jobId)} (F=${f}, C=${c})`); show success banner 6s. On ApiError or network failure: show error banner with message; addLine('error', message). Disable button while submitting. Use Tailwind for styling matching legacy palette. Create `ui/src/components/LogFeed.tsx` with the log card title plus a filter button group (all/info/warn/error) and a "Clear" button. Subscribe to `useLogStore` (lines + filter). Compute filtered lines via the exported selector. Render in a fixed-height (h-56) scrollable container `bg-slate-950 rounded-md font-mono text-xs p-3 overflow-y-auto`. Each line: timestamp in slate-600 + message in level-specific color (info: slate-300, warn: amber-400, error: red-400). Auto-scroll to bottom when new line arrives AND user was already scrolled near bottom (use a ref + useEffect on lines.length). Filter button click calls `useLogStore.getState().setFilter(...)`. Clear button calls `clear()`. Active filter button has bg-slate-700 text-slate-200.
  </action>
  <verify>
    <automated>cd ui &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File ui/src/components/StatsCards.tsx contains "useSystemStore" and "useJobsStore" and "Queue Depth"
    - File ui/src/components/WorkerFleet.tsx contains "useSystemStore" and "currentTaskId" and "shortId"
    - File ui/src/components/SubmitJobForm.tsx contains "createJob" and "upsertJob" and "formatBytes" and "min={2}" or "min=\"2\"" and "max={100000}" or "max=\"100000\""
    - File ui/src/components/SubmitJobForm.tsx contains "ApiError" handling (try/catch) and addLine call with 'error' on failure
    - File ui/src/components/LogFeed.tsx contains "useLogStore" and "setFilter" and at least one of "info" "warn" "error" "all"
    - Command `cd ui &amp;&amp; npm run build` exits 0
    - Command `cd ui &amp;&amp; npm run lint` exits 0
    - Command `grep -n ": any\\|<any>\\| as any" ui/src/components/{StatsCards,WorkerFleet,SubmitJobForm,LogFeed}.tsx 2>/dev/null | wc -l` returns 0
  </acceptance_criteria>
  <done>Stats, worker fleet, submit form, and log feed components implemented and building/linting cleanly. Submit form posts to /jobs and integrates with stores.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: QueueDepthChart and WorkerSpeedChart (Recharts)</name>
  <files>ui/src/components/QueueDepthChart.tsx, ui/src/components/WorkerSpeedChart.tsx</files>
  <read_first>
    - ui/legacy.index.html (lines 229–294 chart config — note legacy used Chart.js; we use Recharts now)
    - ui/src/store/useJobsStore.ts (queueDepthHistory shape: Array<{t:number; depth:number}>)
    - ui/src/store/useSystemStore.ts (workerSpeedHistory shape: Record<string, Array<{t:number; busy:boolean}>>)
  </read_first>
  <action>
    Create `ui/src/components/QueueDepthChart.tsx` titled "Queue Depth (last 2 min)". Subscribe to `queueDepthHistory` from useJobsStore. Filter to entries within `Date.now() - 120000`. Map to data items `{ t: sample.t, ts: new Date(sample.t).toLocaleTimeString(), depth: sample.depth }`. Render a Recharts `<ResponsiveContainer width="100%" height={180}>` containing `<LineChart data={data}>` with `<CartesianGrid stroke="#1e293b" strokeDasharray="3 3" />`, `<XAxis dataKey="ts" tick={{ fontSize: 10, fill: '#64748b' }} hide />`, `<YAxis allowDecimals={false} tick={{ fontSize: 10, fill: '#64748b' }} />`, `<Tooltip contentStyle={{ background: '#1e293b', border: '1px solid #334155' }} labelStyle={{ color:'#94a3b8' }} />`, and `<Line type="monotone" dataKey="depth" stroke="#6366f1" strokeWidth={2} dot={false} isAnimationActive={false} />`. Use a `useEffect` + `setInterval(()=>forceUpdate(), 2000)` (or use `useSyncExternalStore` with a tick) to ensure the chart re-renders on a 2s cadence even when no new SSE events arrive — this preserves the "rolling window" visual that the legacy dashboard had. Create `ui/src/components/WorkerSpeedChart.tsx` titled "Worker Activity (last 2 min)". Subscribe to `workerSpeedHistory` from useSystemStore. For each worker id, compute a rolling 30-second busy-ratio at fixed 5-second buckets across the last 120s: ratio = (count of samples with busy=true within bucket) / (count of samples within bucket). Produce a unified array of `{ ts: string, [workerShortId]: number }` keyed by bucket end time. Render a `<LineChart>` with one `<Line>` per worker using a deterministic color palette (rotate through indigo-500 #6366f1, emerald-500 #10b981, amber-500 #f59e0b, rose-500 #f43f5e, cyan-500 #06b6d4, violet-500 #8b5cf6) keyed by worker id hash modulo palette length. Use the same axes/grid/tooltip styling as QueueDepthChart. Show a `<Legend />` at the bottom with shortId labels. If `workerSpeedHistory` is empty, render placeholder text "No worker activity yet". Same 2s force-refresh pattern as the queue chart.
  </action>
  <verify>
    <automated>cd ui &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File ui/src/components/QueueDepthChart.tsx contains "LineChart" and "ResponsiveContainer" and "queueDepthHistory" and "120000"
    - File ui/src/components/WorkerSpeedChart.tsx contains "LineChart" and "workerSpeedHistory" and "Legend"
    - Both files import from 'recharts'
    - Command `cd ui &amp;&amp; npm run build` exits 0
    - Command `cd ui &amp;&amp; npm run lint` exits 0
    - Command `grep -n ": any\\|<any>\\| as any" ui/src/components/{QueueDepthChart,WorkerSpeedChart}.tsx 2>/dev/null | wc -l` returns 0
  </acceptance_criteria>
  <done>Two Recharts-based chart components render rolling 2-minute windows of queue depth and worker activity, building cleanly with full typing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (form submit) | SubmitJobForm POSTs untrusted F/C from the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | SubmitJobForm F/C inputs | mitigate | Inputs use type=number with min/max attributes matching API Zod bounds (F 2..100000, C 1..10000); client-side guard returns early on invalid input; API enforces same bounds independently |
| T-06-06 | Information Disclosure | LogFeed renders user-controlled message strings from SSE log events | mitigate | React JSX escapes text children by default; rely on text-only rendering (no dangerouslySetInnerHTML) |
| T-06-07 | Denial of Service | Recharts re-renders on tight intervals could leak handlers | mitigate | useEffect returns cleanup that clearInterval()s the 2s refresh tick |
</threat_model>

<verification>
- `cd ui && npm run build` exits 0
- `cd ui && npm run lint` exits 0
- All component files exist and import from stores (no direct API calls except SubmitJobForm using lib/api.ts)
- Zero `any` types in ui/src/components
</verification>

<success_criteria>
- All files in `files_modified` exist
- Components are pure consumers of stores + lib/api (no fetch/EventSource inline)
- Tailwind classes match legacy palette
- Build and lint clean
</success_criteria>

<output>
Create `.planning/phases/DMEAN-06-react-dashboard/06-02-SUMMARY.md` when done.
</output>
