---
phase: 06-react-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/package.json
  - ui/package-lock.json
  - ui/tsconfig.json
  - ui/tsconfig.node.json
  - ui/vite.config.ts
  - ui/tailwind.config.ts
  - ui/postcss.config.js
  - ui/.eslintrc.cjs
  - ui/.gitignore
  - ui/index.html
  - ui/src/main.tsx
  - ui/src/index.css
  - ui/src/vite-env.d.ts
  - ui/src/types/api.ts
  - ui/src/types/sse.ts
  - ui/src/lib/api.ts
  - ui/src/store/useSystemStore.ts
  - ui/src/store/useJobsStore.ts
  - ui/src/store/useLogStore.ts
  - ui/src/hooks/useSSE.ts
  - ui/src/hooks/useInitialLoad.ts
  - ui/App.placeholder.tsx
autonomous: true
requirements:
  - REQ-012
  - REQ-018
  - REQ-019
  - REQ-022
user_setup: []

must_haves:
  truths:
    - "ui/ contains a Vite React 18 + TypeScript project with Tailwind v3 configured"
    - "`npm install` in ui/ succeeds and produces a lockfile"
    - "`npm run build` in ui/ exits 0 with zero TypeScript errors"
    - "Shared API types (Job, Task, WorkerInfo, SystemStats, SSEEvent) are mirrored in ui/src/types"
    - "A typed API client wraps fetch for /jobs, /system, /jobs/:id/tasks, POST /jobs, PATCH /system/workers"
    - "Zustand stores expose system/jobs/log state with strongly-typed actions (no any)"
    - "useSSE hook connects to /events with auto-reconnect and dispatches into stores"
  artifacts:
    - path: "ui/package.json"
      provides: "React 18, TypeScript, Vite, Tailwind v3, Recharts, Zustand pinned"
      contains: "\"react\": \"^18"
    - path: "ui/vite.config.ts"
      provides: "Vite dev server on 5173 with /jobs /system /events /internal proxy to http://localhost:3000"
      contains: "server"
    - path: "ui/tailwind.config.ts"
      provides: "Tailwind v3 with dark slate palette consistent with reference dashboard"
      contains: "content"
    - path: "ui/src/types/api.ts"
      provides: "Job, Task, WorkerInfo, SystemStats, JobStatus, TaskStatus exports"
      contains: "export interface Job"
    - path: "ui/src/types/sse.ts"
      provides: "SSEEvent discriminated union (worker_update | job_update | queue_depth | log | task_completed | connected)"
      contains: "export type SSEEvent"
    - path: "ui/src/lib/api.ts"
      provides: "Typed fetch wrapper: listJobs, getJob, getJobTasks, createJob, getSystem, patchWorkerCount, getResultUrl"
      contains: "export async function createJob"
    - path: "ui/src/store/useSystemStore.ts"
      provides: "Zustand store with workers, queueDepth, connectionStatus + actions"
      contains: "create<SystemState>"
    - path: "ui/src/store/useJobsStore.ts"
      provides: "Zustand store with jobs Record<string, Job>, expandedJobs Set, tasks per job + actions"
      contains: "create<JobsState>"
    - path: "ui/src/store/useLogStore.ts"
      provides: "Zustand store with log lines (capped at 200) and filter level + actions"
      contains: "MAX_LOG_LINES"
    - path: "ui/src/hooks/useSSE.ts"
      provides: "EventSource hook with onopen/onerror/reconnect routing events into stores"
      contains: "EventSource"
    - path: "ui/src/hooks/useInitialLoad.ts"
      provides: "Hook that runs once on mount fetching /jobs + /system to seed stores"
      contains: "useEffect"
  key_links:
    - from: "ui/src/hooks/useSSE.ts"
      to: "/events"
      via: "new EventSource"
      pattern: "EventSource.*\\/events"
    - from: "ui/src/lib/api.ts"
      to: "/jobs"
      via: "fetch wrapper"
      pattern: "\\/jobs"
    - from: "ui/vite.config.ts"
      to: "http://api:3000 (dev proxy)"
      via: "server.proxy"
      pattern: "proxy"
---

<objective>
Scaffold the Vite + React 18 + TypeScript project under `ui/`, configure Tailwind v3, Recharts, and Zustand, and build the typed foundation that all UI components will consume: API client, shared types, Zustand stores, SSE hook, and initial-load hook.

Purpose: Interface-first construction. Subsequent plans (02 components, 03 jobs table, 04 app shell + tests + docker) consume these stores/types directly — no codebase exploration required.

Output: A buildable but visually empty React app with full state plumbing wired to the existing API at http://localhost:3000.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Reference implementation (source of truth for behavior — DO NOT delete; replaced in Plan 04)
@ui/index.html

# API contracts (mirror these as TS types)
@api/src/types/index.ts
@api/src/routes/jobs.ts
@api/src/routes/system.ts

<interfaces>
<!-- Mirror these contracts into ui/src/types/api.ts exactly. Server uses Date objects; over JSON they serialize to ISO strings. -->

From api/src/types/index.ts:
- JobStatus = 'generating' | 'queued' | 'running' | 'aggregating' | 'done' | 'failed'
- TaskStatus = 'pending' | 'running' | 'done' | 'failed'
- Job { id, f, c, status, batchCount, completedBatches, resultPath, error, createdAt, updatedAt, completedAt }
  NOTE: On the wire createdAt/updatedAt/completedAt are ISO strings. In UI types, declare them as `string | null`.
- Task { id, jobId, batchIndex, fileStart, fileEnd, status, workerId, startedAt, completedAt, error }
- WorkerInfo { id, status: 'idle' | 'busy', currentTaskId: string | null }
- SystemStats { workers, workerCount, idleWorkers, busyWorkers, queueDepth, jobStats: { total, generating, queued, running, aggregating, done, failed } }
- SSE events: worker_update | job_update | queue_depth | log | (additionally: task_completed and connected — present in dashboard usage but not in original union; include them in ui/src/types/sse.ts)

API endpoints (base = http://localhost:3000 in dev via Vite proxy):
- GET  /jobs                  → { jobs: Job[] }
- POST /jobs   body {F,C}     → 202 { jobId, f, c, status, batchCount, createdAt }
- GET  /jobs/:id              → Job
- GET  /jobs/:id/tasks        → { tasks: Task[] } (verify response shape — fall back to array if response is itself an array)
- GET  /jobs/:id/result       → CSV stream (used as href for download link)
- GET  /system                → SystemStats
- PATCH /system/workers body {count:number 1..20} → { ok, targetWorkerCount }
- GET  /events                → SSE stream (also at /system/events)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold Vite + React 18 + TS project and install deps</name>
  <files>ui/package.json, ui/package-lock.json, ui/tsconfig.json, ui/tsconfig.node.json, ui/vite.config.ts, ui/tailwind.config.ts, ui/postcss.config.js, ui/.eslintrc.cjs, ui/.gitignore, ui/index.html, ui/src/main.tsx, ui/src/index.css, ui/src/vite-env.d.ts, ui/App.placeholder.tsx</files>
  <read_first>
    - ui/index.html (current vanilla dashboard — KEEP UNCHANGED in this plan; it is the live dashboard until Plan 04 replaces it)
    - api/src/routes/system.ts (to confirm /events path and CORS Access-Control-Allow-Origin: *)
    - .planning/ROADMAP.md (Phase 6 deliverables)
    - docker-compose.yml (api service exposes 3000 — vite proxy targets http://localhost:3000)
  </read_first>
  <action>
    Move the existing `ui/index.html` to `ui/legacy.index.html` so the new `ui/index.html` Vite entry can take its place; the legacy file remains as a reference. Create `ui/package.json` with name `distributed-mean-ui`, type `module`, scripts: `dev` → `vite`, `build` → `tsc -b && vite build`, `preview` → `vite preview --port 5173`, `lint` → `eslint . --max-warnings 0`, `test` → `vitest run`, `test:coverage` → `vitest run --coverage`, `check` → `tsc -b && npm run lint && npm run test:coverage`. Pin dependencies: react ^18.3, react-dom ^18.3, zustand ^4.5, recharts ^2.12, clsx ^2.1. devDependencies: typescript ^5.5, @types/react ^18.3, @types/react-dom ^18.3, @vitejs/plugin-react ^4.3, vite ^5.4, tailwindcss ^3.4, postcss ^8.4, autoprefixer ^10.4, eslint ^9, @typescript-eslint/parser ^8, @typescript-eslint/eslint-plugin ^8, eslint-plugin-react-hooks ^5, eslint-plugin-react-refresh ^0.4, vitest ^2, @vitest/coverage-v8 ^2, @testing-library/react ^16, @testing-library/jest-dom ^6, @testing-library/user-event ^14, jsdom ^25. Create `ui/tsconfig.json` extending @tsconfig style: target ES2022, lib ["ES2022","DOM","DOM.Iterable"], module ESNext, moduleResolution Bundler, jsx react-jsx, strict true, noUncheckedIndexedAccess true, exactOptionalPropertyTypes true, noImplicitOverride true, noFallthroughCasesInSwitch true, isolatedModules true, useDefineForClassFields true. Forbid `any` via @typescript-eslint/no-explicit-any error. Create `ui/tsconfig.node.json` for vite.config.ts. Create `ui/vite.config.ts` exporting defineConfig with plugins [react()], server { port 5173, host true, proxy: '/jobs' '/system' '/events' '/internal' all → http://localhost:3000 with changeOrigin true, and `/events` must NOT buffer (set `configure: (proxy) => proxy.on('proxyRes', (res) => res.headers['cache-control'] = 'no-cache')` or simply rely on `ws:false` and SSE working through default proxy). Set test config inline: { environment: 'jsdom', setupFiles: './src/setupTests.ts', globals: true, coverage: { provider: 'v8', reporter: ['text','html','lcov'], thresholds: { lines: 60, branches: 60, functions: 60, statements: 60 } } }. Create `ui/tailwind.config.ts` with content ['./index.html','./src/**/*.{ts,tsx}'] and theme.extend.colors matching the reference (slate-900/800/700 palette; accents indigo-500, emerald-500, amber-500, red-500). Create `ui/postcss.config.js` with tailwindcss + autoprefixer. Create `ui/.eslintrc.cjs` extending typescript-eslint recommended-type-checked + react-hooks recommended with rules `@typescript-eslint/no-explicit-any: error`, `no-console: ['warn',{allow:['warn','error']}]`. Create `ui/.gitignore` with node_modules, dist, coverage, .vite, .DS_Store. Create `ui/index.html` (new Vite entry) with `<div id="root">` and `<script type="module" src="/src/main.tsx">`, page title "Distributed Mean — Dashboard", and meta viewport. Create `ui/src/main.tsx` importing './index.css' and rendering `<App />` from `./App` into root with React.StrictMode. Create `ui/src/index.css` with the three @tailwind directives plus a body rule using bg-slate-900 text-slate-200 font-sans antialiased. Create `ui/src/vite-env.d.ts` with `/// <reference types="vite/client" />`. Create a temporary `ui/App.placeholder.tsx` exporting a minimal `<div>Phase 6 scaffold</div>` component AND export it from `ui/src/App.tsx` (re-export `export { default } from '../App.placeholder'`) — this is a temporary stub so the project builds. Plans 02–04 will replace App.tsx. Run `npm install` inside `ui/` to produce package-lock.json. Do NOT delete ui/legacy.index.html — it remains until Plan 04.
  </action>
  <verify>
    <automated>cd ui && npm install &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File ui/package.json exists and contains substrings "\"react\": \"^18", "\"vite\":", "\"tailwindcss\": \"^3", "\"recharts\":", "\"zustand\":", "\"vitest\":"
    - File ui/tsconfig.json contains "\"strict\": true" and "\"noUncheckedIndexedAccess\": true"
    - File ui/vite.config.ts contains "proxy" and "http://localhost:3000" and "5173"
    - File ui/tailwind.config.ts contains "./src/**/*.{ts,tsx}"
    - File ui/.eslintrc.cjs contains "@typescript-eslint/no-explicit-any" set to "error"
    - File ui/index.html contains `id="root"` and `src="/src/main.tsx"`
    - File ui/src/main.tsx contains `React.StrictMode` and `createRoot`
    - File ui/src/index.css contains "@tailwind base"
    - Command `cd ui &amp;&amp; npm run build` exits 0 and produces ui/dist/index.html
    - File ui/legacy.index.html exists (renamed from old ui/index.html)
    - Command `grep -nE "\\bany\\b" ui/src 2>/dev/null | grep -v "^Binary" | wc -l` returns 0
  </acceptance_criteria>
  <done>Vite + React 18 + TS + Tailwind + Recharts + Zustand project scaffolded under ui/, `npm install` and `npm run build` both succeed with zero errors and zero `any` types in src/.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mirror API types and build typed fetch client</name>
  <files>ui/src/types/api.ts, ui/src/types/sse.ts, ui/src/lib/api.ts</files>
  <read_first>
    - api/src/types/index.ts (mirror Job/Task/WorkerInfo/SystemStats/SSEEventType exactly — but Date fields become `string | null` over JSON)
    - api/src/routes/jobs.ts (response shapes: POST /jobs returns 202 with subset, GET /jobs returns { jobs }, GET /jobs/:id returns full Job, GET /jobs/:id/tasks returns { tasks })
    - api/src/routes/system.ts (GET /system returns SystemStats; PATCH /system/workers expects {count:int 1..20})
    - api/src/index.ts (note that /events is mounted both at /system/events and as an alias /events)
  </read_first>
  <action>
    Create `ui/src/types/api.ts` exporting: JobStatus union of 'generating'|'queued'|'running'|'aggregating'|'done'|'failed'; TaskStatus union 'pending'|'running'|'done'|'failed'; interface Job with all fields from api/src/types/index.ts but createdAt, updatedAt, completedAt typed as `string | null` (ISO timestamps over JSON; completedAt nullable); interface Task with startedAt/completedAt as `string | null`; interface WorkerInfo identical to API; interface SystemStats identical including nested jobStats. Add helper type `CreateJobRequest = { F: number; C: number }` and `CreateJobResponse = { jobId: string; f: number; c: number; status: JobStatus; batchCount: number; createdAt: string }`. Add `PatchWorkersRequest = { count: number }` and `PatchWorkersResponse = { ok: true; targetWorkerCount: number }`. Create `ui/src/types/sse.ts` exporting SSEEvent discriminated union: `{ type: 'worker_update'; workers: WorkerInfo[] }` | `{ type: 'job_update'; job: Partial<Job> & { id: string } }` | `{ type: 'queue_depth'; depth: number }` | `{ type: 'log'; level: 'info'|'warn'|'error'; message: string; timestamp: string }` | `{ type: 'task_completed' }` | `{ type: 'connected' }`. Add `LogLevel = 'info'|'warn'|'error'` export. Create `ui/src/lib/api.ts` with a `BASE` constant resolved from `import.meta.env.VITE_API_BASE ?? ''` (empty string defaults to relative URLs so Vite proxy routes work in dev and nginx handles in prod). Export async functions: `listJobs(): Promise<Job[]>` calls `GET /jobs` and unwraps `{jobs}`; `getJob(id): Promise<Job>` calls `GET /jobs/:id`; `getJobTasks(id): Promise<Task[]>` calls `GET /jobs/:id/tasks` and unwraps `{tasks}` (defensive: also accept Task[] directly); `createJob({F,C}): Promise<CreateJobResponse>` POSTs JSON; `getSystem(): Promise<SystemStats>` calls `GET /system`; `patchWorkerCount(count): Promise<PatchWorkersResponse>` PATCHes JSON; `getResultUrl(id): string` returns `${BASE}/jobs/${id}/result`. All functions must throw `ApiError extends Error` (also exported) containing `status` number and parsed body for non-2xx responses; non-JSON 5xx bodies should be captured as `body: text`. No `any` anywhere; use `unknown` plus narrowing where needed.
  </action>
  <verify>
    <automated>cd ui &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File ui/src/types/api.ts contains "export interface Job" and "export interface Task" and "export interface SystemStats" and "createdAt: string | null"
    - File ui/src/types/sse.ts contains "export type SSEEvent" and all six discriminants ('worker_update','job_update','queue_depth','log','task_completed','connected')
    - File ui/src/lib/api.ts contains "export async function createJob", "export async function listJobs", "export async function getSystem", "export async function patchWorkerCount", "export class ApiError", "export function getResultUrl"
    - File ui/src/lib/api.ts contains "/jobs" and "/system" and "method: 'POST'" and "method: 'PATCH'"
    - Command `grep -n ": any" ui/src/types ui/src/lib 2>/dev/null | wc -l` returns 0
    - Command `cd ui &amp;&amp; npm run build` exits 0
  </acceptance_criteria>
  <done>Typed mirrors of API contracts exist in ui/src/types and a fully typed fetch client exists in ui/src/lib/api.ts with no `any` usages. Project builds cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build Zustand stores and SSE hook</name>
  <files>ui/src/store/useSystemStore.ts, ui/src/store/useJobsStore.ts, ui/src/store/useLogStore.ts, ui/src/hooks/useSSE.ts, ui/src/hooks/useInitialLoad.ts, ui/src/setupTests.ts, ui/App.placeholder.tsx</files>
  <read_first>
    - ui/legacy.index.html (lines 217–351 — reference state model: workers, queueDepth, jobs, logLines, logFilter, taskCountHistory, expandedJobs, and the handleEvent switch statement; lines 600–633 — initial load via /jobs and /system in parallel; lines 296–323 — SSE connect with EventSource onopen/onerror/reconnect at 3000ms)
    - ui/src/types/api.ts (consumed by stores)
    - ui/src/types/sse.ts (consumed by useSSE)
    - ui/src/lib/api.ts (consumed by useInitialLoad)
  </read_first>
  <action>
    Create `ui/src/store/useSystemStore.ts` with Zustand: state `{ workers: WorkerInfo[], queueDepth: number, connectionStatus: 'connecting'|'connected'|'reconnecting', workerSpeedHistory: Record<string, Array<{ t: number; busy: boolean }>> }`. Actions: `setWorkers(workers)` (replaces workers AND appends a sample to workerSpeedHistory for each worker keyed by id, capped to entries within last 120000 ms), `setQueueDepth(n)`, `setConnectionStatus(s)`. Note: worker "speed" is derived later as busy-ratio over a rolling 2-minute window — store raw busy samples here. Create `ui/src/store/useJobsStore.ts` with state `{ jobs: Record<string, Job>, jobTasks: Record<string, Task[]>, expandedJobs: Set<string>, queueDepthHistory: Array<{ t: number; depth: number }> }`. Actions: `upsertJob(partial: Partial<Job> & {id:string})` deep-merges into existing entry, `setJobs(list)`, `setJobTasks(jobId, tasks)`, `toggleExpanded(jobId)`, `pushQueueDepthSample(t, depth)` (cap to entries within last 120000 ms). Selector helpers exported: `selectJobList(state)` returns sorted by createdAt desc, `selectJobStats(state)` returns counts by status. Create `ui/src/store/useLogStore.ts` with `MAX_LOG_LINES = 200`, state `{ lines: Array<{ id: string; level: LogLevel; message: string; ts: string }>, filter: 'all' | LogLevel }`. Actions: `addLine(level, message, timestamp?)` pushes and trims to 200, `setFilter(f)`, `clear()`. Use `crypto.randomUUID()` for id. Selector `selectFilteredLines(state)` returns filtered. Create `ui/src/hooks/useSSE.ts` exporting `useSSE()` hook with no args. On mount: open `new EventSource('/events')`; on open call `setConnectionStatus('connected')`; on message parse JSON as SSEEvent and route: worker_update → systemStore.setWorkers(workers); job_update → jobsStore.upsertJob(job); queue_depth → systemStore.setQueueDepth(depth) + jobsStore.pushQueueDepthSample(Date.now(), depth); log → logStore.addLine(level, message, timestamp); task_completed → no-op (or reserved counter); connected → no-op. On error: setConnectionStatus('reconnecting'), close the EventSource, setTimeout reconnect after 3000ms (track timer id; clear on unmount). On unmount: close EventSource and clear timer. Use refs to track current EventSource and timer to avoid stale closures. Create `ui/src/hooks/useInitialLoad.ts` exporting `useInitialLoad()` hook: on mount, run `Promise.all([listJobs(), getSystem()])`; seed `useJobsStore.setState({ jobs: Object.fromEntries(jobs.map(j => [j.id, j])) })`; seed `useSystemStore.setState({ workers: sys.workers, queueDepth: sys.queueDepth, connectionStatus: 'connecting' })`; on failure call `useLogStore.getState().addLine('warn', \`Initial load failed: \${err.message}\`)`. Create `ui/src/setupTests.ts` importing `@testing-library/jest-dom/vitest`. Update `ui/App.placeholder.tsx` to call both `useSSE()` and `useInitialLoad()` from a small App component, render a status pill `<span>{connectionStatus}</span>` reading from useSystemStore so the wiring is exercised at runtime. Re-export from `ui/src/App.tsx` unchanged. All actions and selectors must be fully typed; zero `any`.
  </action>
  <verify>
    <automated>cd ui &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File ui/src/store/useSystemStore.ts contains "create" and "setWorkers" and "queueDepth" and "workerSpeedHistory"
    - File ui/src/store/useJobsStore.ts contains "upsertJob" and "expandedJobs" and "queueDepthHistory" and "toggleExpanded"
    - File ui/src/store/useLogStore.ts contains "MAX_LOG_LINES = 200" and "addLine"
    - File ui/src/hooks/useSSE.ts contains "new EventSource" and "/events" and "setTimeout" and "3000"
    - File ui/src/hooks/useSSE.ts contains switch cases or branches for 'worker_update', 'job_update', 'queue_depth', 'log', 'task_completed'
    - File ui/src/hooks/useInitialLoad.ts contains "listJobs" and "getSystem" and "Promise.all"
    - Command `grep -rn ": any\\b\\|<any>\\| as any" ui/src 2>/dev/null | wc -l` returns 0
    - Command `cd ui &amp;&amp; npm run build` exits 0
    - Command `cd ui &amp;&amp; npm run lint` exits 0
  </acceptance_criteria>
  <done>Three Zustand stores plus useSSE and useInitialLoad hooks exist with full type safety. App.placeholder.tsx invokes both hooks so a manual `npm run dev` connects to /events and seeds state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (HTTP/SSE) | Untrusted user input crosses here via POST /jobs and PATCH /system/workers |
| dev proxy → API | Vite dev server proxies untrusted browser requests to localhost:3000 |
| npm registry → build | Third-party packages pulled at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | POST /jobs F/C inputs from browser | mitigate | API already enforces Zod schema (F 2..100000, C 1..10000); UI additionally clamps via input min/max — defense in depth |
| T-06-02 | Information Disclosure | SSE log events surfaced to UI | accept | API curates `broadcastLog` content (no PII; local dev environment) |
| T-06-03 | Denial of Service | EventSource reconnect loop on API down | mitigate | useSSE uses fixed 3000ms backoff and clears timer on unmount; no unbounded reconnect rate |
| T-06-04 | Elevation of Privilege | PATCH /system/workers callable from any browser session | accept | Dev-only system; API has no auth boundary in v1 (documented in PROJECT.md) |
| T-06-SC | Tampering | npm package installs (react, recharts, zustand, tailwindcss, vite, vitest, etc.) | mitigate | All packages are well-known public packages from the established React ecosystem; lockfile committed; legitimacy checkpoint waived per RESEARCH.md (packages predate phase research and are listed as standard stack in ROADMAP) |
</threat_model>

<verification>
- `cd ui && npm install` succeeds and creates package-lock.json
- `cd ui && npm run build` exits 0 with zero TypeScript errors
- `cd ui && npm run lint` exits 0 with zero warnings
- `grep -rn ": any\b\|<any>\| as any" ui/src` returns no matches
- Manual smoke (optional): `cd ui && npm run dev` then visit http://localhost:5173 — page renders the connection status text from useSystemStore and the network tab shows an open EventSource on /events
</verification>

<success_criteria>
- All files in `files_modified` exist
- `npm run build` and `npm run lint` both pass in `ui/`
- Zero `any` types in `ui/src`
- ui/legacy.index.html preserved (old dashboard still works via API static-serve until Plan 04 wires the new dashboard)
</success_criteria>

<output>
Create `.planning/phases/DMEAN-06-react-dashboard/06-01-SUMMARY.md` when done.
</output>
