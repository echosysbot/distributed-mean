---
phase: 06-react-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - ui/src/components/JobsTable.tsx
  - ui/src/components/JobTaskRows.tsx
  - ui/src/components/StatusBadge.tsx
  - ui/src/hooks/useJobTasks.ts
autonomous: true
requirements:
  - REQ-020
  - REQ-025

must_haves:
  truths:
    - "Jobs table renders all jobs from useJobsStore sorted newest first"
    - "Each row is expandable; expanding fetches /jobs/:id/tasks lazily and caches into useJobsStore.jobTasks"
    - "Columns are sortable by createdAt (default), status, f, c, progress; clicking header toggles sort direction"
    - "Done jobs render a download link to GET /jobs/:id/result; non-done show '—'"
    - "Status badges color-coded per legacy palette for both jobs and tasks"
  artifacts:
    - path: "ui/src/components/JobsTable.tsx"
      provides: "Sortable expandable jobs table"
      contains: "useJobsStore"
    - path: "ui/src/components/JobTaskRows.tsx"
      provides: "Task detail rows rendered when a job is expanded"
      contains: "fileStart"
    - path: "ui/src/components/StatusBadge.tsx"
      provides: "Reusable colored badge for JobStatus and TaskStatus"
      contains: "status"
    - path: "ui/src/hooks/useJobTasks.ts"
      provides: "Hook that lazy-loads tasks when a job is expanded"
      contains: "getJobTasks"
  key_links:
    - from: "ui/src/components/JobsTable.tsx"
      to: "ui/src/store/useJobsStore.ts"
      via: "selectors + toggleExpanded"
      pattern: "toggleExpanded"
    - from: "ui/src/hooks/useJobTasks.ts"
      to: "/jobs/:id/tasks"
      via: "api.getJobTasks"
      pattern: "getJobTasks"
---

<objective>
Implement the sortable, expandable jobs table and its supporting task-rows component, status badge, and lazy-fetch hook.

Purpose: Isolated from Plan 02 (no file conflicts) so it can run in parallel. Plan 04 will compose this into the App shell alongside Plan 02 components.

Output: A self-contained jobs table that drives off useJobsStore and lazily loads task breakdowns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/DMEAN-06-react-dashboard/06-01-SUMMARY.md
@ui/legacy.index.html
@ui/src/types/api.ts
@ui/src/store/useJobsStore.ts
@ui/src/lib/api.ts
@ui/src/lib/format.ts

<interfaces>
<!-- All data access through useJobsStore and api.getJobTasks. -->
<!-- Selectors available from Plan 01: selectJobList, selectJobStats. -->
<!-- Store actions: toggleExpanded(jobId), setJobTasks(jobId, tasks), upsertJob(partial). -->

Job status palette (match legacy badges):
- generating → bg-sky-900 text-sky-300
- queued → bg-stone-900 text-stone-300
- running → bg-emerald-950 text-emerald-300
- aggregating → bg-violet-950 text-violet-300
- done → bg-emerald-950 text-emerald-400
- failed → bg-red-950 text-red-300

Task status palette:
- pending → bg-stone-900 text-stone-300
- running → bg-emerald-950 text-emerald-300
- done → bg-emerald-950 text-emerald-400
- failed → bg-red-950 text-red-300

API: getJobTasks(id) returns Task[]; getResultUrl(id) returns the download href.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: StatusBadge + useJobTasks hook</name>
  <files>ui/src/components/StatusBadge.tsx, ui/src/hooks/useJobTasks.ts</files>
  <read_first>
    - ui/legacy.index.html (lines 66–73 badge styles; lines 434–449 toggleTasks lazy-load pattern)
    - ui/src/types/api.ts (JobStatus and TaskStatus unions)
    - ui/src/store/useJobsStore.ts (jobTasks Record and expandedJobs Set, setJobTasks action)
    - ui/src/lib/api.ts (getJobTasks signature)
  </read_first>
  <action>
    Create `ui/src/components/StatusBadge.tsx` accepting props `{ status: JobStatus | TaskStatus; kind?: 'job' | 'task' }`. Map status → Tailwind classes per the interfaces table above. Render `<span class="inline-block rounded-full px-2 py-0.5 text-xs font-semibold {colorClasses}">{status}</span>`. Default kind is 'job'. Fully typed; use Record<JobStatus, string> and Record<TaskStatus, string> for the color maps. Create `ui/src/hooks/useJobTasks.ts` exporting `useJobTasks(jobId: string, enabled: boolean)`. Internal state: { loading: boolean, error: string | null }. When `enabled` flips from false → true AND `useJobsStore.getState().jobTasks[jobId]` is undefined, call `getJobTasks(jobId)`; on resolve call `useJobsStore.getState().setJobTasks(jobId, tasks)`; on reject set local error and call `useLogStore.getState().addLine('warn', \`Failed to load tasks for \${shortId(jobId)}: \${err.message}\`)`. Return `{ loading, error }`. Use a ref to track in-flight requests to avoid duplicate fetches.
  </action>
  <verify>
    <automated>cd ui &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File ui/src/components/StatusBadge.tsx contains "JobStatus" and "TaskStatus" and "Record<JobStatus" or "Record<TaskStatus"
    - File ui/src/hooks/useJobTasks.ts contains "getJobTasks" and "setJobTasks" and "enabled"
    - Command `cd ui &amp;&amp; npm run build` exits 0
    - Command `grep -n ": any\\|<any>\\| as any" ui/src/components/StatusBadge.tsx ui/src/hooks/useJobTasks.ts | wc -l` returns 0
  </acceptance_criteria>
  <done>StatusBadge renders job/task statuses in correctly colored badges; useJobTasks lazy-loads tasks once per job and caches into the store.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: JobTaskRows component</name>
  <files>ui/src/components/JobTaskRows.tsx</files>
  <read_first>
    - ui/legacy.index.html (lines 410–419 task row rendering with files {start}–{end}, worker id, elapsed)
    - ui/src/types/api.ts (Task fields)
    - ui/src/components/StatusBadge.tsx
    - ui/src/lib/format.ts (formatElapsed, shortId)
  </read_first>
  <action>
    Create `ui/src/components/JobTaskRows.tsx` accepting props `{ jobId: string; tasks: Task[] | undefined; loading: boolean; error: string | null; colSpan: number }`. If `loading` is true and tasks is undefined: render a single `<tr><td colSpan={colSpan}>Loading tasks…</td></tr>` styled bg-slate-900 text-slate-500 italic. If `error` is non-null: render a single `<tr><td colSpan={colSpan} class="text-red-400">{error}</td></tr>`. Else map over tasks rendering one `<tr>` per task with cells: short task id (mono), `<StatusBadge status={t.status} kind="task" />`, "files {fileStart}–{fileEnd}" spanning 2 cols, worker shortId or '—', elapsed (`formatElapsed(t.startedAt, t.completedAt)` or 'running…' if startedAt && !completedAt, else '—'), and an empty cell for the result column. Use `<tr class="bg-slate-950 text-xs">` and `<td class="px-3 py-1.5 border-b border-slate-800">`. Indent first cell with pl-10.
  </action>
  <verify>
    <automated>cd ui &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File ui/src/components/JobTaskRows.tsx contains "fileStart" and "fileEnd" and "StatusBadge" and "formatElapsed"
    - File ui/src/components/JobTaskRows.tsx contains all three branches: loading, error, and tasks list
    - Command `cd ui &amp;&amp; npm run build` exits 0
    - Command `grep -n ": any\\|<any>\\| as any" ui/src/components/JobTaskRows.tsx | wc -l` returns 0
  </acceptance_criteria>
  <done>JobTaskRows renders the loading/error/list states for task breakdowns with proper styling.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: JobsTable with sort + expand</name>
  <files>ui/src/components/JobsTable.tsx</files>
  <read_first>
    - ui/legacy.index.html (lines 379–432 renderJobs, lines 393–408 main row markup with progress bar, lines 434–449 toggleTasks)
    - ui/src/store/useJobsStore.ts (selectJobList, toggleExpanded, expandedJobs, jobTasks)
    - ui/src/lib/api.ts (getResultUrl)
    - ui/src/components/StatusBadge.tsx
    - ui/src/components/JobTaskRows.tsx
    - ui/src/hooks/useJobTasks.ts
    - ui/src/lib/format.ts (formatElapsed, shortId)
  </read_first>
  <action>
    Create `ui/src/components/JobsTable.tsx`. Local state: `sortKey: 'createdAt' | 'status' | 'f' | 'c' | 'progress'` (default 'createdAt') and `sortDir: 'asc' | 'desc'` (default 'desc'). Subscribe to `jobs` via useJobsStore and compute `jobList` with `useMemo`: convert to array, then sort by sortKey/sortDir (progress = completedBatches/batchCount or 0). Subscribe to `expandedJobs` and `jobTasks` via useJobsStore. If jobList is empty, render `<div class="text-slate-500 text-center py-8">No jobs yet — submit one above!</div>`. Else render `<table class="w-full border-collapse text-sm">` with `<thead>` showing columns: "Job ID" (not sortable), "Status" (sort 'status'), "F" (sort 'f'), "C" (sort 'c'), "Progress" (sort 'progress'), "Duration" (sort 'createdAt' as proxy), "Result" (not sortable). Sortable headers use a button-like style and show ▴/▾ when active. Click handler updates sortKey/sortDir (if same key, toggle direction; else set new key with desc). For each job render two rows wrapped: a main row that is `cursor-pointer hover:bg-slate-900`, clicking calls `useJobsStore.getState().toggleExpanded(j.id)`. Main row cells: chevron `▾` if expanded else `▸` + ` ${shortId(j.id)}`; `<StatusBadge status={j.status} />`; `{j.f}`; `{j.c}`; progress text `${completedBatches ?? 0}/${batchCount ?? '?'}` plus a progress bar `<div class="h-1 bg-slate-700 rounded mt-1"><div class="h-full bg-gradient-to-r from-indigo-500 to-violet-500 rounded" style={{width: \`${pct}%\`}} /></div>`; duration via `formatElapsed(j.createdAt, j.completedAt)`; result column shows `<a href={getResultUrl(j.id)} download class="text-indigo-400 text-xs">⬇ CSV</a>` only when `j.status === 'done'`, else `—`. After the main row, conditionally render `<JobTaskRows>` when `expandedJobs.has(j.id)` is true; obtain tasks/loading/error from `useJobTasks(j.id, expanded)`. colSpan = 7. Use `<React.Fragment key={j.id}>` to wrap both rows. Anchor link clicks must NOT propagate to the row click; add `onClick={(e) => e.stopPropagation()}` to the download anchor.
  </action>
  <verify>
    <automated>cd ui &amp;&amp; npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File ui/src/components/JobsTable.tsx contains "toggleExpanded" and "sortKey" and "sortDir" and "getResultUrl"
    - File ui/src/components/JobsTable.tsx contains "useJobTasks" and "JobTaskRows" and "StatusBadge"
    - File ui/src/components/JobsTable.tsx contains "expandedJobs" and "stopPropagation"
    - Command `cd ui &amp;&amp; npm run build` exits 0
    - Command `cd ui &amp;&amp; npm run lint` exits 0
    - Command `grep -n ": any\\|<any>\\| as any" ui/src/components/JobsTable.tsx | wc -l` returns 0
  </acceptance_criteria>
  <done>JobsTable renders sortable headers, expandable rows with lazy-loaded tasks, and proper download link behavior. Builds and lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (lazy GET) | useJobTasks fetches /jobs/:id/tasks on demand |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Denial of Service | Lazy task fetch could fire repeatedly on rapid expand/collapse | mitigate | useJobTasks uses an in-flight ref and the store-cached jobTasks to dedupe; only fetches when enabled && !cached && !in-flight |
| T-06-09 | Information Disclosure | Job IDs in result download URLs are unguessable UUIDs | accept | UUIDs are not secrets in this dev system; consistent with existing API behavior |
</threat_model>

<verification>
- `cd ui && npm run build` exits 0
- `cd ui && npm run lint` exits 0
- All files exist; zero `any` types in this plan's files
</verification>

<success_criteria>
- JobsTable, JobTaskRows, StatusBadge, useJobTasks exist and build cleanly
- No file conflict with Plan 02 (confirmed by disjoint files_modified)
- All store interactions go through Plan 01's selectors/actions
</success_criteria>

<output>
Create `.planning/phases/DMEAN-06-react-dashboard/06-03-SUMMARY.md` when done.
</output>
